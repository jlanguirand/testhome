<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Behold! The Awesomeness of [Your Name Here]</title>
    <!-- Tailwind CSS CDN for modern styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts - Inter for a clean, modern look -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        /* Custom styles to enhance Tailwind, ensuring Inter font is applied */
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); /* A cool, subtle gradient background */
            min-height: 100vh; /* Ensure body takes full viewport height */
            display: flex;
            flex-direction: column;
            color: #e2e8f0; /* Light text color for contrast */
            overflow-x: hidden; /* Prevent horizontal scroll */
        }
        /* Custom scrollbar for a cleaner look */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #4a5568;
        }
        ::-webkit-scrollbar-thumb {
            background: #81e6d9;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #4fd1c5;
        }

        /* Keyframe for a subtle pulse animation on elements */
        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 0 0 rgba(0, 0, 0, 0);
            }
            50% {
                transform: scale(1.02);
                box-shadow: 0 0 20px rgba(129, 230, 217, 0.6);
            }
        }

        /* Apply pulse animation to specific elements */
        .animate-pulse-subtle {
            animation: pulse 3s infinite ease-in-out;
        }

        /* Keyframe for a dazzling text shadow effect */
        @keyframes dazzling-text {
            0% { text-shadow: 0 0 5px #81e6d9, 0 0 10px #4fd1c5; }
            25% { text-shadow: 0 0 10px #4fd1c5, 0 0 20px #38b2ac; }
            50% { text-shadow: 0 0 15px #38b2ac, 0 0 30px #2c7a7b; }
            75% { text-shadow: 0 0 10px #4fd1c5, 0 0 20px #38b2ac; }
            100% { text-shadow: 0 0 5px #81e6d9, 0 0 10px #4fd1c5; }
        }

        .dazzling-text {
            animation: dazzling-text 4s infinite alternate;
        }

        /* Background particle effect - Enhanced for a starfield look */
        .particle-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
            z-index: -1; /* Send to background */
        }

        .particle {
            position: absolute;
            background-color: rgba(255, 255, 255, 0.4); /* Brighter particles */
            border-radius: 50%;
            opacity: 0;
            animation: floatAndFade 15s infinite ease-in-out; /* Longer duration for smoother float */
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5); /* Subtle glow */
        }

        @keyframes floatAndFade {
            0% {
                transform: translateY(100vh) scale(0); /* Start from bottom of screen */
                opacity: 0;
            }
            20% {
                opacity: 1;
                transform: translateY(80vh) scale(1);
            }
            80% {
                opacity: 1;
                transform: translateY(20vh) scale(1.2); /* Slightly larger in middle */
            }
            100% {
                transform: translateY(-10vh) scale(0); /* Go slightly above top and fade out */
                opacity: 0;
            }
        }

        /* NEW: Animated background overlay for a crazy effect */
        .animated-background-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2; /* Even further back than particles */
            background: radial-gradient(circle at center, rgba(129, 230, 217, 0.15) 0%, transparent 70%); /* Subtle glowing circle */
            animation: swirlAndPulse 20s infinite linear; /* Apply the new animation */
        }

        @keyframes swirlAndPulse {
            0% {
                transform: scale(1) rotate(0deg);
                opacity: 0.8;
                filter: blur(0px);
            }
            25% {
                transform: scale(1.1) rotate(90deg);
                opacity: 0.9;
                filter: blur(2px);
            }
            50% {
                transform: scale(1) rotate(180deg);
                opacity: 0.8;
                filter: blur(0px);
            }
            75% {
                transform: scale(0.9) rotate(270deg);
                opacity: 0.7;
                filter: blur(2px);
            }
            100% {
                transform: scale(1) rotate(360deg);
                opacity: 0.8;
                filter: blur(0px);
            }
        }
    </style>
</head>
<body class="flex flex-col min-h-screen">
    <!-- Particle background container -->
    <div class="particle-container"></div>
    <!-- NEW: Animated background overlay for a crazy effect -->
    <div class="animated-background-overlay"></div>

    <!-- Header: The Grand Entrance -->
    <header class="relative bg-gradient-to-r from-purple-700 to-indigo-800 text-white py-20 px-4 sm:px-6 lg:px-8 text-center shadow-2xl overflow-hidden rounded-b-3xl">
        <!-- Abstract background shapes for visual interest -->
        <div class="absolute inset-0 z-0 opacity-20">
            <svg class="w-full h-full" viewBox="0 0 1440 320" preserveAspectRatio="none">
                <path fill="#a78bfa" fill-opacity="1" d="M0,160L48,176C96,192,192,224,288,208C384,192,480,128,576,122.7C672,117,768,171,864,186.7C960,203,1056,181,1152,170.7C1248,160,1344,160,1392,160L1440,160L1440,0L1392,0C1344,0,1248,0,1152,0C1056,0,960,0,864,0C768,0,672,0,576,0C480,0,384,0,288,0C192,0,96,0,48,0L0,0Z"></path>
            </svg>
        </div>
        <div class="relative z-10">
            <h1 class="text-5xl sm:text-6xl lg:text-7xl font-extrabold mb-4 dazzling-text">
                Witness True Greatness!
            </h1>
            <p class="text-xl sm:text-2xl lg:text-3xl font-light mb-6">
                Prepare to be amazed by the unparalleled awesomeness of <span class="font-bold text-teal-300">[Your Name Here]</span>
            </p>
            <button id="uncover-button" class="bg-teal-400 hover:bg-teal-500 text-purple-900 font-bold py-3 px-8 rounded-full shadow-lg transform hover:scale-105 transition duration-300 ease-in-out animate-pulse-subtle">
                Uncover My Legendary Status
            </button>
        </div>
    </header>

    <!-- Main Content: Sections of Awesomeness -->
    <main class="flex-grow container mx-auto px-4 sm:px-6 lg:px-8 py-12">

        <!-- Section 1: The Origin Story of Awesome -->
        <section id="intro" class="bg-white bg-opacity-10 rounded-2xl p-8 mb-12 shadow-xl border border-purple-500 transform hover:rotate-1 transition duration-300 ease-in-out">
            <h2 class="text-4xl font-bold text-center text-teal-300 mb-6 dazzling-text">The Genesis of Genius</h2>
            <p class="text-lg leading-relaxed text-center max-w-3xl mx-auto">
                From the moment I emerged, it was clear: the universe had a new benchmark for excellence. My journey has been a symphony of triumphs, a ballet of breakthroughs, and an epic saga of <span class="font-bold text-yellow-300">sheer, unadulterated brilliance</span>. Every step, every thought, every breath has been a testament to what happens when awesomeness takes human form.
            </p>
        </section>

        <!-- NEW: Hidden Section to be revealed -->
        <section id="hidden-revelation" class="hidden bg-white bg-opacity-20 rounded-2xl p-8 mb-12 shadow-2xl border border-yellow-500 text-center transform scale-95 opacity-0 transition-all duration-700 ease-out">
            <h2 class="text-5xl font-extrabold text-yellow-300 mb-6 dazzling-text">The Secret Scroll of Supreme Skills!</h2>
            <p class="text-xl leading-relaxed max-w-4xl mx-auto mb-8">
                You've dared to peek behind the curtain of ordinary existence! What you see here are the *truly* top-secret, mind-bending abilities that defy mere mortal comprehension. Prepare for your reality to be redefined.
            </p>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-left">
                <div class="bg-white bg-opacity-10 p-5 rounded-xl border border-yellow-400">
                    <h3 class="text-2xl font-bold text-teal-200 mb-2">Quantum Leap Thinker</h3>
                    <p class="text-lg text-gray-200">My thoughts don't just connect dots; they create new dimensions where dots can exist. Future problems? Already solved, last Tuesday.</p>
                </div>
                <div class="bg-white bg-opacity-10 p-5 rounded-xl border border-yellow-400">
                    <h3 class="text-2xl font-bold text-teal-200 mb-2">Reality Warper (Minor)</h3>
                    <p class="text-lg text-gray-200">Sometimes, if I concentrate hard enough, I can make traffic lights turn green. Don't ask how. It's awesome.</p>
                </div>
                <div class="bg-white bg-opacity-10 p-5 rounded-xl border border-yellow-400 col-span-full">
                    <h3 class="text-2xl font-bold text-teal-200 mb-2">Infinite Energy Source (Personal)</h3>
                    <p class="text-lg text-gray-200">My enthusiasm alone could power a small city. My actual energy? Let's just say the sun gets jealous.</p>
                </div>
            </div>
        </section>
        <!-- END NEW Hidden Section -->

        <!-- Section 2: My Superpowers (Skills) -->
        <section id="skills" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
            <!-- Skill Card 1 -->
            <div class="bg-white bg-opacity-15 rounded-2xl p-6 shadow-2xl border border-indigo-400 transform hover:-translate-y-2 transition duration-300 ease-in-out">
                <h3 class="text-2xl font-semibold text-teal-200 mb-4 flex items-center">
                    <span class="mr-2 text-3xl">✨</span> Innovation Architect
                </h3>
                <p class="text-md text-gray-200">I don't just think outside the box; I've redesigned the box, built a spaceship out of it, and launched it into a new dimension of possibilities.</p>
                <div class="w-full bg-gray-700 rounded-full h-2.5 mt-4">
                    <div class="bg-teal-400 h-2.5 rounded-full" style="width: 99.99%;"></div> <!-- Almost 100% because perfection is always evolving -->
                </div>
            </div>
            <!-- Skill Card 2 -->
            <div class="bg-white bg-opacity-15 rounded-2xl p-6 shadow-2xl border border-indigo-400 transform hover:-translate-y-2 transition duration-300 ease-in-out">
                <h3 class="text-2xl font-semibold text-teal-200 mb-4 flex items-center">
                    <span class="mr-2 text-3xl">🧠</span> Problem Annihilator
                </h3>
                <p class="text-md text-gray-200">Problems tremble in my presence. I solve them before they even know they're problems, often with a casual flick of the wrist.</p>
                <div class="w-full bg-gray-700 rounded-full h-2.5 mt-4">
                    <div class="bg-teal-400 h-2.5 rounded-full" style="width: 98%;"></div>
                </div>
            </div>
            <!-- Skill Card 3 -->
            <div class="bg-white bg-opacity-15 rounded-2xl p-6 shadow-2xl border border-indigo-400 transform hover:-translate-y-2 transition duration-300 ease-in-out">
                <h3 class="text-2xl font-semibold text-teal-200 mb-4 flex items-center">
                    <span class="mr-2 text-3xl">🗣️</span> Charisma Dynamo
                </h3>
                <p class="text-md text-gray-200">My words are pure gold, my presence electrifying. I can charm the socks off a robot, then teach it to tap dance.</p>
                <div class="w-full bg-gray-700 rounded-full h-2.5 mt-4">
                    <div class="bg-teal-400 h-2.5 rounded-full" style="width: 99%;"></div>
                </div>
            </div>
            <!-- Add more skill cards as needed -->
        </section>

        <!-- Section 3: Legendary Achievements -->
        <section id="achievements" class="bg-white bg-opacity-10 rounded-2xl p-8 mb-12 shadow-xl border border-purple-500">
            <h2 class="text-4xl font-bold text-center text-teal-300 mb-8 dazzling-text">My Pantheon of Prowess</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Achievement Card 1 -->
                <div class="bg-white bg-opacity-15 rounded-xl p-6 shadow-lg flex items-start space-x-4 border border-indigo-300">
                    <span class="text-5xl">🏆</span>
                    <div>
                        <h3 class="text-2xl font-semibold text-yellow-200 mb-2">Revolutionized [Industry/Field]</h3>
                        <p class="text-gray-200">Single-handedly redefined the landscape of [specific area], leaving competitors in awe and history books scrambling to update.</p>
                    </div>
                </div>
                <!-- Achievement Card 2 -->
                <div class="bg-white bg-opacity-15 rounded-xl p-6 shadow-lg flex items-start space-x-4 border border-indigo-300">
                    <span class="text-5xl">🚀</span>
                    <div>
                        <h3 class="text-2xl font-semibold text-yellow-200 mb-2">Invented the [Cool Thing]</h3>
                        <p class="text-gray-200">My brainchild, the [Cool Thing], has since become an indispensable part of modern existence. You're welcome.</p>
                    </div>
                </div>
                <!-- Achievement Card 3 -->
                <div class="bg-white bg-opacity-15 rounded-xl p-6 shadow-lg flex items-start space-x-4 border border-indigo-300">
                    <span class="text-5xl">🌟</span>
                    <div>
                        <h3 class="text-2xl font-semibold text-yellow-200 mb-2">Mastered [Complex Skill] in record time</h3>
                        <p class="text-gray-200">They said it couldn't be done. I did it. While juggling flaming chainsaws. Blindfolded. (Figuratively, mostly.)</p>
                    </div>
                </div>
                <!-- Achievement Card 4 -->
                <div class="bg-white bg-opacity-15 rounded-xl p-6 shadow-lg flex items-start space-x-4 border border-indigo-300">
                    <span class="text-5xl">👑</span>
                    <div>
                        <h3 class="text-2xl font-semibold text-yellow-200 mb-2">Became a [Title] of Pure Awesomeness</h3>
                        <p class="text-gray-200">My reputation precedes me, often arriving via a fanfare of trumpets and a shower of glitter.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section 4: Fun Facts (with a rotator) -->
        <section id="fun-facts" class="bg-white bg-opacity-10 rounded-2xl p-8 mb-12 shadow-xl border border-purple-500 text-center">
            <h2 class="text-4xl font-bold text-teal-300 mb-6 dazzling-text">Did You Know?</h2>
            <div id="fact-display" class="text-2xl font-light text-yellow-200 mb-4 min-h-[60px] flex items-center justify-center">
                <!-- Facts will be inserted here by JavaScript -->
            </div>
            <button id="next-fact-btn" class="bg-teal-500 hover:bg-teal-600 text-white font-bold py-2 px-6 rounded-full shadow-md transform hover:scale-105 transition duration-300 ease-in-out">
                Another Mind-Blowing Fact!
            </button>
        </section>

        <!-- Section 5: Connect with the Legend -->
        <section id="contact" class="bg-white bg-opacity-10 rounded-2xl p-8 shadow-xl border border-purple-500 text-center">
            <h2 class="text-4xl font-bold text-teal-300 mb-6 dazzling-text">Connect with Awesomeness</h2>
            <p class="text-lg mb-8 max-w-2xl mx-auto">
                Ready to bask in the glow of my unparalleled brilliance? Or perhaps you have a challenge worthy of my attention? Reach out!
            </p>
            <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
                <a href="mailto:your.email@example.com" class="bg-yellow-400 hover:bg-yellow-500 text-purple-900 font-bold py-3 px-8 rounded-full shadow-lg transform hover:scale-105 transition duration-300 ease-in-out animate-pulse-subtle">
                    Email Me (If You Dare!)
                </a>
                <a href="https://linkedin.com/in/yourprofile" target="_blank" class="bg-indigo-400 hover:bg-indigo-500 text-white font-bold py-3 px-8 rounded-full shadow-lg transform hover:scale-105 transition duration-300 ease-in-out">
                    LinkedIn (Prepare to be Impressed)
                </a>
            </div>
        </section>

    </main>

    <!-- Footer: The End of a Chapter, Not the Awesomeness -->
    <footer class="bg-gray-900 bg-opacity-50 text-center py-8 px-4 sm:px-6 lg:px-8 mt-12 rounded-t-3xl shadow-inner">
        <p class="text-gray-400 text-sm">
            &copy; <span id="current-year"></span> The Legend of [Your Name Here]. All rights reserved. (But really, who could ever replicate this?)
        </p>
        <div class="flex justify-center space-x-6 mt-4">
            <a href="#" class="text-gray-400 hover:text-teal-300 transition duration-300 ease-in-out">Privacy Policy</a>
            <a href="#" class="text-gray-400 hover:text-teal-300 transition duration-300 ease-in-out">Terms of Awesomeness</a>
        </div>
    </footer>

    <!-- JavaScript for dynamic elements -->
    <script>
        // Set current year in footer
        document.getElementById('current-year').textContent = new Date().getFullYear();

        // Fun Facts Rotator
        const funFacts = [
            "I once taught a squirrel to code in assembly language. It's now a lead developer.",
            "My coffee brews itself, out of sheer respect.",
            "I can parallel park a spaceship on the first try.",
            "My shadow has its own fan club.",
            "I'm fluent in sarcasm, ancient Sumerian, and the language of dolphins.",
            "Even my typos are grammatically perfect.",
            "I've counted to infinity. Twice.",
            "My ideas are so good, they often spontaneously generate confetti.",
            "I can make a grown-up cry... tears of joy, of course.",
            "My smile is known to increase local market value."
        ];
        let currentFactIndex = 0;
        const factDisplay = document.getElementById('fact-display');
        const nextFactBtn = document.getElementById('next-fact-btn');

        function displayFact() {
            factDisplay.style.opacity = 0; // Fade out
            setTimeout(() => {
                factDisplay.textContent = funFacts[currentFactIndex];
                factDisplay.style.opacity = 1; // Fade in
            }, 300); // Match this to CSS transition duration if any
        }

        nextFactBtn.addEventListener('click', () => {
            currentFactIndex = (currentFactIndex + 1) % funFacts.length;
            displayFact();
        });

        // Initial fact display
        displayFact();

        // Automatic fact rotation (optional)
        setInterval(() => {
            currentFactIndex = (currentFactIndex + 1) % funFacts.length;
            displayFact();
        }, 8000); // Change fact every 8 seconds

        // Particle background effect
        const particleContainer = document.querySelector('.particle-container');
        function createParticle() {
            const particle = document.createElement('div');
            particle.classList.add('particle');
            const size = Math.random() * 5 + 2; // Size between 2px and 7px
            particle.style.width = `${size}px`;
            particle.style.height = `${size}px`;
            particle.style.left = `${Math.random() * 100}vw`;
            particle.style.animationDuration = `${Math.random() * 10 + 5}s`; // Duration between 5s and 15s
            particle.style.animationDelay = `-${Math.random() * 10}s`; // Random start delay
            particleContainer.appendChild(particle);

            // Remove particle after animation to prevent DOM bloat
            particle.addEventListener('animationend', () => {
                particle.remove();
            });
        }

        // Generate a continuous stream of particles (increased frequency for more "crazy")
        setInterval(createParticle, 100); // Create a new particle every 100ms

        // NEW: Logic for "Uncover My Legendary Status" button
        const uncoverButton = document.getElementById('uncover-button');
        const hiddenRevelationSection = document.getElementById('hidden-revelation');

        uncoverButton.addEventListener('click', () => {
            // Toggle the 'hidden' class
            hiddenRevelationSection.classList.toggle('hidden');

            // Add/remove classes for a smooth reveal animation
            if (!hiddenRevelationSection.classList.contains('hidden')) {
                // If it's now visible, trigger the reveal animation
                setTimeout(() => {
                    hiddenRevelationSection.classList.remove('scale-95', 'opacity-0');
                    hiddenRevelationSection.classList.add('scale-100', 'opacity-100');
                    // Scroll to the revealed section
                    hiddenRevelationSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }, 10); // Small delay to allow 'hidden' to be removed before transition starts
            } else {
                // If it's becoming hidden, reverse the animation before hiding
                hiddenRevelationSection.classList.remove('scale-100', 'opacity-100');
                hiddenRevelationSection.classList.add('scale-95', 'opacity-0');
                // After the transition, truly hide it
                setTimeout(() => {
                    hiddenRevelationSection.classList.add('hidden');
                }, 700); // Match this to the transition duration
            }
        });
    </script>
</body>
</html>
